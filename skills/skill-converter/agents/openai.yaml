interface:
  display_name: "Skill Converter"
  short_description: "Port external agent specs into local skills"
  default_prompt: "Use $skill-converter to adapt an upstream agent spec, prompt file, or role document into a local skill while keeping the core workflow neutral and the repository-specific packaging minimal."
